```py
import requests             # 网页请求 -- requests.get(), post()
import csv                  # csv操作 -- csv.DictWriter(), writeheader(), writerows()
from lxml import html       # 原始网页解析 -- html.fromstring(html原始网页), xpath(xpath表达式)
from pathlib import Path    # 文件io路径操作 -- Path(__file__).resolve().path 获取代码所在文件的父级路径
import re                   # 正则表达式
```